import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Photo Gallery - Apexion Tech Solutions",
  description: "A modern photo gallery showcasing beautiful images with smooth navigation and mobile-optimized design.",
  keywords: "photo gallery, images, photography, mobile-friendly, modern design",
  authors: [{ name: "Apexion Tech Solutions" }],
  viewport: "width=device-width, initial-scale=1",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
